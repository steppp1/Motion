name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server
        env:
          PRIVATE_KEY: ${{ secrets.SSH_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          sudo apt-get update && sudo apt-get install -y ssh
          mkdir -p ~/.ssh/
          echo "$PRIVATE_KEY" > ~/.ssh/ForGithub.pem
          chmod 600 ~/.ssh/ForGithub.pem
          ssh-keyscan -t rsa,ecdsa,ed25519 -H $SERVER_IP >> ~/.ssh/known_hosts
          # 指定私钥登录
          ssh -o HostKeyAlgorithms=+ssh-ed25519,ssh-rsa,ecdsa-sha2-nistp256 $SERVER_USER@$SERVER_IP << 'EOF'
            cd ~/Motion
            git pull origin master
            docker compose down
            docker compose up -d --build
          EOF
